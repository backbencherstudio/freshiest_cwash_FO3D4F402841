import 'package:flutter/material.dart';

class FeedbackFormCard extends StatelessWidget{
  const FeedbackFormCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("How is your Car Wash Experience")
      ],
    );
  }
}